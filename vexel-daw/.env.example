# Engine Mode Configuration
# Copy this file to .env to customize settings
#
# ENGINE_MODE: Determines which engine backend to use
#   - mock: Use Electron IPC with mock engine (default for development)
#   - ipc:  Use native engine via IPC (future implementation)
#
VITE_ENGINE_MODE=mock
